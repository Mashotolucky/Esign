


const deleteIntepreter = async (id) => {
    try {

      return 1;///await deleteIntepreterDb(id);

    } catch (error) {
      throw error;
    }
};

 const createIntepreter = async (id) => {
    try {

     return 1;// await deleteIntepreterDb(id);

    } catch (error) {
      throw error;
    }
};